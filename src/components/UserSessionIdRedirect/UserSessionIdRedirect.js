import React from 'react';
import PropTypes from 'prop-types';
import './UserSessionIdRedirect.scss';

const UserSessionIdRedirect = () => (
  <div className="UserSessionIdRedirect">
    UserSessionIdRedirect Component
  </div>
);

UserSessionIdRedirect.propTypes = {};

UserSessionIdRedirect.defaultProps = {};

export default UserSessionIdRedirect;
